VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_API_BASE=http://127.0.0.1:8000/api/v1

# Optional. Used to build the password-reset redirect URL
# (VITE_SITE_URL + /reset-password). Leave unset for local dev and
# Vercel previews -- it falls back to window.location.origin. Set it
# explicitly only for the production deployment, e.g.:
# VITE_SITE_URL=https://adanse.app
VITE_SITE_URL=
